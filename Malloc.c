#include<stdio.h>
#include<stdlib.h>

signed int b = 20;
int main(){
	
	int n, k;
	int* ptr = (int *)malloc(n * sizeof(int));
	if(ptr == NULL){
		printf("Memory not allowed");
	}
	else {
		printf("Memory allocation successful")
		
		for(int i 9)
	}
}
